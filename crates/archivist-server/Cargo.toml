[package]
name = "archivist-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

# LAN salt-okuma dagitim/bildirim sunucusu (S1-S2 MVP). H2 lan_server.rs guvenlik iskeletinin
# H3'e portu: 8-hane auth kodu + IP rate-limit + constant-time + siki CORS. Senkron (tiny_http,
# kendi thread'inde doner) → H3'un senkron rusqlite/Tauri-komut modeliyle uyumlu (tokio GEREKMEZ).
# DB'yi SAHIPLENMEZ: bildirimleri enjekte edilen bir closure ile ceker (src-tauri DB'yi acar).

[dependencies]
tiny_http.workspace = true
getrandom.workspace = true
serde.workspace = true
serde_json.workspace = true
