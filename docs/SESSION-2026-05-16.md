# Oturum Özeti — 2026-05-16 (v2.4.9 hattı)

> Yarın devam için ÖNCE bunu oku. Tüm iş `main`'de (v2.4.9). **v3 (`feat/v3-2-vec-db`) bu oturumda HİÇ dokunulmadı.**
> `main` = `origin/main` = `deb51d3` (senkron, push'lu). Baseline: `d2709c0`.

## Nasıl başladı
V249-0 (CSP warmup) düzeltme isteğiyle başladı; bu makinede `npm run tauri dev` ortamı bozuk
çıkınca arka arkaya **yayınlanmış v2.4.9'da uyuyan** kusurlar açığa çıktı ve düzeltildi.
Hiçbiri v3/bu oturum kaynaklı değil; hepsi pre-existing latent defect.

## Bugün kapatılanlar (7 commit, hepsi main'de + push)
| Commit | İş | Doğrulama |
|---|---|---|
| `999d319` | **V249-0** CLIP warmup `data:`→Blob (CSP→WebGPU stall) | Prod'da doğrulandı (warmup logu, CSP hatası yok) |
| `e343f76` | **V249-5/6** transformers v4.2.0 offline: asset.localhost `remoteHost` + fp32 yoksa q8 (ERROR_CODE 7) | Prod: `embeds=74/74`, fp32/WebGPU çalıştı |
| `e56ccde` | Görsel Ara modal: detay paneli + CSV Export | Kullanıcı onayladı |
| `4d010a8` | RAG keyword fallback Türkçe (`tr_norm` SQL fn; SQLite ASCII LOWER bug) | 2105/2105 + gerçek DB |
| `b1445ed` | **Bloat:** pre-2.4.8 ölü `dwg_shapes` tablosu migration ile DROP+VACUUM | Gerçek arşiv: 127.3 MB → **1.2 MB (%99)**, veri korundu |
| `3251476` | **Keyword-gate:** birebir tüm-token eşleşen chunk topK'ya garanti (~0.99) | Gerçek DB sim: "şenay" → sgkHizmetDokumu.pdf [1] 0.990 |
| `deb51d3` | Sızan prompt iskeleti temizliği + **düşünme-göster toggle** (varsayılan KAPALI) | 2111/2111; kullanıcı kurdu/test etti |

Test: tsc temiz, `npm test` **2111/2111**.

## "Şenay" sorununun durumu (önemli ayrım)
- **Retrieval = ÇÖZÜLDÜ ve kanıtlandı.** Kök neden: bu arşivde `fts_chunks` YOK → zayıf LIKE
  fallback + SQLite ASCII LOWER Türkçe'yi kaçırıyordu; ayrıca füzyon skoru exact-isim
  eşleşmesini topK dışına itiyordu. `tr_norm` + keyword-gate ile çözüldü; gerçek arşivde
  `3d dizayn yetki.docx` / `sgkHizmetDokumu_59329062220.pdf` artık [1] skor 0.990.
- **AÇIK / OPSİYONEL (madde 3 — kullanıcı erteledi):** qwen3:8b doğru kaynağı alıyor ama
  zayıf sentez yapıyor / prompt'u kusuyordu. (1) prompt-çöp temizliği + (2) düşünme toggle
  yapıldı (deb51d3). Geriye kalan **model/prompt kalitesi**: ya daha güçlü model ya prompt
  sadeleştirme. Kullanıcı kararı bekliyor. `ollama list` + model tercihi sorulacak.

## Yarın için kritik notlar / tuzaklar
- **`npm run tauri dev` BU MAKİNEDE embedding tarafında BOZUK** (transformers/asset.localhost).
  Doğrulama HER ZAMAN **prod build** ile yapılmalı, dev ile değil. (Bu oturumda en büyük zaman kaybı buydu.)
- Test arşivi `C:\test_arşiv_DB\archivist.db` benim tarafımdan temizlendi (127→1.2 MB).
  Yedek: `C:\test_arşiv_DB\archivist.db.bak_20260516` — kullanıcı isterse silebilir.
- Bloat migration idempotent + tek-seferlik VACUUM; her pre-2.4.8 arşiv ilk açılışta otomatik küçülür.
- Düşünme toggle LS-kalıcı (`archivist_show_thinking`), varsayılan KAPALI; ChatHeader ⚙️ menüsünde.
- Son installer: `src-tauri/target/release/bundle/{msi,nsis}/ArchivistPro_2.4.9_x64*` (16 May 17:57/18:00, = `deb51d3`).

## Yarın olası adımlar
1. Kullanıcı 1+2'nin prod test sonucunu paylaşır → teyit/ince ayar.
2. Madde 3 kararı: model değiştir (qwen list'e göre) **veya** prompt sadeleştir (chat-çekirdek, dikkatli + testli).
3. İstenirse: `archivist.db.bak_20260516` temizliği, docs/TODO.md'ye V249 kapanış notu.
