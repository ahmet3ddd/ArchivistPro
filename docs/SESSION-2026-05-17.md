# Oturum Özeti — 2026-05-17 (v2.4.9 hattı)

> Devam için ÖNCE bunu oku. Tüm iş `main`'de. `main` baseline: `5ea3cad`, yeni HEAD: `76da9dc`.
> **Push EDİLMEDİ** (kullanıcı kararı bekliyor — cross-PC için gerekirse `git push`).
> v3 dalına dokunulmadı.

## Nasıl başladı
"qwen3:8b zayıf sentez (madde 3)" + "daha iyi eşleşme" istendi. Önce model değişimi
denendi, sonra asıl retrieval kusuru bulunup düzeltildi.

## 1) qwen2.5:7b-instruct denemesi — **ELENDİ** (git'te iz yok, tam revert)
- Hipotez: qwen3 hibrit-düşünme sentezi bozuyor; düşünme-modsuz instruct daha temiz olur.
- `DEFAULT_CHAT_MODEL` → qwen2.5:7b-instruct + v2.4.10 bump + prod build yapıldı.
- **Kullanıcı prod testi (1.png/2.png):** qwen2.5 cevabı "yetersiz, sığ" — agresif
  "KISA/DİREKT/tek satır" prompt'u (qwen3'ü susturmak için sertleştirilmiş) qwen2.5
  fazla itaatkâr olduğu için çıplak/citation'sız cevaba indiriyor. qwen3:8b'nin
  gevşek itaati daha zengin+kaynaklı cevap veriyor → kullanıcı qwen3:8b'yi yeğledi.
- **Karar:** qwen3:8b'ye tam dönüldü (6 dosya `git restore`, HEAD=5ea3cad'e eşit).
  **Bu yolu TEKRAR DENEME.** Model revize edilecekse: gemma2:9b ya da prompt-sadeleştirme
  (model+prompt eşleşik — qwen3-susturan prompt altında başka model adil değerlendirilemez).

## 2) Önceki handoff düzeltmesi
2026-05-16 handoff/hafıza "retrieval ÇÖZÜLDÜ ve kanıtlandı" diyordu — bu **aşırı iddiaydı**.
"Gerçek DB sim" `fts_chunks` OLMAYAN arşivde test etmişti; gerçek all-scope app akışı farklı.

## 3) ASIL kök neden bulundu + düzeltildi — commit `76da9dc`
- Belirti: "X **kim**" çalışıyor (RAG içerik yolu) ama "X **hangi belgede var**" "bulunamadı".
- Kök neden: `ragService.ts directFileListAnswer` (list-intent / LLM-bypass yolu) yalnız
  `buildFullSearchableText`'i (dosya adı/proje/etiket/metadata) tarıyor, **`text_chunks`
  belge METNİ taranmıyordu**. İçerikte olup dosya adında olmayan terim → hep "bulunamadı".
  Üç yönden doğrulandı: kod yapısı + birebir görsel mesajı + DB içeriği (kopya üzerinde
  salt-okunur tanı: `C:\test_arşiv_DB\archivist.db`, fts_chunks YOK, 86 text_chunks,
  tr_norm LIKE "şenay"ı buluyor).
- Fix: `ftsSearchChunks` (FTS5 + tr_norm fallback, kanıtlı) ile aday chunk'lar çekilip
  **anlamlı token'ların TÜMÜ birebir geçen** belgeler eklendi (keyword-gate kesinliği).
  Sadece metadata'nın kaçırdığını ekler; mevcut davranış korunur. +6 test.
- **Doğrulama:** tsc temiz, **2117/2117**. Kullanıcı `npm run tauri dev`'de doğruladı:
  "müthiş hızlı ve doğru cevaplar".

## Kritik içgörü (tuzaktan kurtaran)
`directFileListAnswer` list-intent yolu **embedding'e HİÇ dokunmaz** (loadEmbeddingModel/
generateEmbedding yok). Bu makinedeki dev-embedding arızası bu yolu **etkilemez** →
"hangi/nerede/içeren/listele" sorguları **dev'de test edilebilir**, MSI gerekmez.
(RAG-sentez/embedding doğrulaması hâlâ DAİMA prod build ile.)

## Açık / temizlik
- **Stale artefakt:** `src-tauri/target/release/bundle/{msi,nsis}/ArchivistPro_2.4.10_x64*`
  (qwen2.5, obsolet) — silinebilir. Pull'lanan `qwen2.5:7b-instruct` ollama modeli (4.7GB,
  kullanılmıyor) — `ollama rm qwen2.5:7b-instruct` ile yer açılır (ya da gemma2/prompt
  denemesi için tutulur).
- **Latent risk (bu arşivde TETİKLENMİYOR):** `database.ts:4139` `if (results.size===0)` —
  tr_norm LIKE fallback yalnız FTS5 sıfır dönerse çalışır. Bu test arşivinde fts_chunks YOK
  (FTS5 throw → results boş → fallback çalışır → sorun yok). Ama fts_chunks OLAN bir arşivde
  FTS5 ascii-tokenizer Türkçe-yanlış+boş-olmayan dönerse fallback atlanır → Türkçe isim
  kaçabilir. İleride fts_chunks'lı arşivde Türkçe-isim kaçışı görülürse buraya bak.
- **Push:** yapılmadı; cross-PC için `git push` gerekir.

## Geçerli konvansiyonlar
Chat/retrieval-çekirdek: [[feedback_preserve_chat_fixes]], renk token [[project_chat_ui_color_tokens]],
çalışma stili [[feedback_work_style]].

## GÜNCELLEME (oturum sonu — yukarıdaki "push edilmedi" notu ARTIK GEÇERSİZ)
- **Push YAPILDI.** `main` HEAD `849d5b6` origin'de. Annotated tag **`v2.4.10`** atıldı + push'landı.
- **v2.4.10 yayınlandı:** `849d5b6 chore(release): v2.4.10` — sürüm 2.4.9→2.4.10 (4 dosya + Cargo.lock
  senkron), tarih 2026-05-17. Kapsam: retrieval fix `76da9dc` (önceki v2.4.9 MSI bunu içermiyordu).
- **Temizlik YAPILDI:** qwen2.5 modeli + eski (qwen2.5'li, obsolet) v2.4.10 bundle'ları silindi (785 MB).
  Şimdi build edilen v2.4.10 = **meşru release** (retrieval fix'li, qwen3:8b).
- **Final MSI:** `npm run tauri build` v2.4.10 için koşturuldu → `…/bundle/{msi,nsis}/ArchivistPro_2.4.10_x64*`.
- **SIRADAKİ: v3'e geçiş.** v3 dalı `feat/v3-2-vec-db`. **v3'ün İLK işi kod değil, ıraksama
  değerlendirmesidir:** `feat/v3-2-vec-db` main'den ne zaman ayrıldı, bu retrieval fix
  (`76da9dc`) + 2026-05-16'daki 7 fix + öncekiler v3'te VAR MI? Yoksa main→v3 merge/port
  gerekir. Uzun-ömürlü ıraksak dal + aylarca biriken main fix'leri = klasik tuzak; önce bunu ölç.
