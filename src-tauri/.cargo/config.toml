[profile.dev]
# Reduce debug info level to avoid LNK1318 (PDB size overflow) on Windows
# 1 = line tables only (vs 2 = full debug info)
debug = 1
# Disable incremental compilation to avoid stale PDB state
incremental = false

[profile.dev.build-override]
debug = false
